
spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #配置Nacos地址
  devtools:
    restart:
      enabled: false
management:
  endpoints:
    web:
      exposure:
        include: '*'
